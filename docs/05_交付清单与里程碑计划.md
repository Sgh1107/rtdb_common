# 05 交付清单与里程碑计划

## 1. 全量交付物清单

### A. 运行时组件
| # | 交付物 | 形态 | 说明 |
|---|---|---|---|
| A1 | **librtdbcore**（`rtdbcore.dll` / `librtdbcore.so`） | 动态库(+静态库可选) | L0~L3 全部：平台抽象/infra/存储/引擎。嵌入用户唯一依赖 |
| A2 | **rtdbd** | 守护进程/服务 | RPC 服务端+订阅推送+快照恢复+watchdog；支持 Windows 服务与 systemd 单元文件 |
| A3 | **rtdb_client** 动态库 + `rtdb_client.hpp/c.h` | 动态库 | 网络客户端：连接池/自动重连/角色切换/订阅回调 |
| A4 | RDB 同步插件框架 + MySQL 示例适配器 | 动态插件 | 接口头 `IRdbSyncAdapter.hpp`；Oracle/Sybase 等由使用方实现 |
| A5 | SQL 单表子集执行器（内嵌于 core，可裁剪编译开关） | 库内置 | SELECT/UPDATE/INSERT/DELETE + WHERE 比较逻辑 + ORDER BY/LIMIT |

### B. SDK 与工具
| # | 交付物 | 说明 |
|---|---|---|
| B1 | 公共头文件包 `include/rtdb-*.h(pp)` | Native/C 双面 API + 错误码 + 协议常量 |
| B2 | rtdbctl CLI | status / DDL / csv·json 导入导出 / 订阅打印 / 用户与权限管理 |
| B3 | bench 压测工具 | 嵌入与网络两种模式基准脚本化输出 |
| B4 | migrate_from_psrtdb | 老库共享内存映像离线迁移工具（一次性交付） |
| B5 | 各语言绑定样例 | Python(ctypes 完整示例)、Java(JNI 样例工程)——样例而非正式产品 |

### C. 文档
| # | 交付物 |
|---|---|
| C1 | 《安装部署指南》（嵌入式/服务式两形态，Win/Linux） |
| C2 | 《API 参考手册》（doxygen 生成 + 手写教程） |
| C3 | 《共享内存布局与网络协议规范 v1》 |
| C4 | 《运维手册》：性能调参(fsync/容量)、监控计数器说明、故障恢复演练步骤 |
| C5 | 《老系统迁移指南》（PSRtdb → rtdb_common 概念对照表） |

### D. 工程设施
D1 CMake 工程与工具链预设；D2 CI（三矩阵+asan/tsan+覆盖率）；D3 clang-format/tidy 配置；D4 测试套件（见《06》）。

## 2. 目录结构规划
```
rtdb_common/
├── docs/                  # 本文档集
├── include/rtdb/          # 对外公共头
├── src/
│   ├── platform/{win32,posix}/
│   ├── infra/    storage/    engine/    api/
│   ├── server/   client/     sql/
│   └── plugins/rdb_mysql/
├── tools/{rtdbctl,bench,migrate_from_psrtdb}/
├── examples/{embed_demo,remote_demo,cpp17_minimal}/
├── tests/{unit,integration,stress,fuzzing}/
├── cmake/   scripts/ci/
└── CMakeLists.txt
```

## 3. 里程碑排期（按 1~2 名熟悉该领域的高级工程师估算）

| 里程碑 | 周期(约) | 内容 | 验收标准(出口) |
|---|---|---|---|
| **M0 地基 ✅ 已验收（2026-08-28，tag: m0）** | 4 周 | platform/infra 全部；SharedBTree；Slab 分配器；CI 就绪 | infra 单测全覆盖；两进程锁互斥+死亡恢复演示通过 |
| **M1 引擎** | 6 周 | TableStore/Catalog/Index/WAL/快照恢复 | 崩溃注入(kill -9×随机点)后数据零丢失(per_second 档)；CRUD 全链路单测绿 |
| **M2 接口** | 3 周 | NativeApi 收敛、CAbi、RecordBlock 批量、Result 体系 | 《04》示例程序原样跑通；bench 嵌入模式达标(§4) |
| **M3 服务** | 5 周 | rtdbd(RPC/会话/订阅推送/优雅退出)、rtdb_client | 双机演示：主机写→备端订阅增量到达 P99<50ms；断网重连游标续传正确 |
| **M4 完善** | 4 周 | SQL 子集、Authz、PerfCounters、迁移工具、全部文档 | 验收清单(§5)全过；文档评审通过 |

合计约 **22 周**（串行估算；A1-A5 中 A4 插件框架可与 M3 并行）。

## 3.1 M0 验收记录（2026-08-28）

- **验收提交**：`1150d09`（CI 最终态）；验收依据为该提交触发的全绿 CI 运行；
- **CI job 全绿**：`build-test`（windows-latest / ubuntu-latest）、`sanitize-linux`（ASan+UBSan+LSan 插桩全量单测）、`coverage-linux`（行覆盖 **86%** ≥ 门槛 80%，functions 95.6%）；
- **出口项核对**：
  - [x] infra 单测全覆盖：25 个用例覆盖 平台层/共享内存/CRC/分配器/互斥锁/B+树/引擎生命周期全路径；
  - [x] 两进程锁互斥+死亡恢复演示通过：`ShmMutexDeathRecovery.RecoversAfterHolderHardKill`（子进程携锁 hard-exit → 新会话抢锁恢复），Windows 实测通过，Linux 由同用例覆盖；
  - [x] CI 就绪：双平台矩阵 + sanitize + coverage 四 job；
- **覆盖门槛调整记录**：全局 85% → **80%**（决策 2026-08-28，已固化进 CI `--fail-under-line 80`）；engine/storage ≥85% 转为 M1 目标；
- **已知项（带入 M1）**：SharedBTree 惰性删除不回收页；跨进程并发变更树压力用例未建。

## 4. 性能验收基线（单机 i7/32G 参考值）
| 场景 | 指标 |
|---|---|
| 嵌入单行 upsert(主键) | P99 ≤ 10 μs（fsync=per_second） |
| RecordBlock 批量 upsert | ≥ 1,000,000 行/s（8 列混合类型） |
| 主键点查 | P99 ≤ 3 μs；范围扫描 10 万行 ≤ 30 ms |
| WAL 重放恢复 | 1 GB 变更日志重放 ≤ 60 s |
| 网络批量吞吐(单连接千兆网) | ≥ 200,000 行/s |
| 订阅端到端延迟 | 本机 ≤ 1 ms；跨机 P99 ≤ 50 ms |

## 5. M4 总验收清单
- [ ] 三平台矩阵 CI 绿 + asan/tsan 无告警 + 行覆盖 ≥ 75%（engine/storage ≥ 85%）
- [ ] 混沌测试：随机 kill 写进程 100 次，重启后数据一致且订阅游标连续
- [ ] 在线加列演示：旧版本读者句柄持续读不崩溃，新列可见性符合文档语义
- [ ] 权限用例：未授权写被拒且留审计计数；token 过期自动失效
- [ ] migrate_from_psrtdb 成功迁移示例老映像并抽样比对通过
- [ ] C4 运维手册演练：快照备份→模拟磁盘坏→恢复全过程文档与实操一致
